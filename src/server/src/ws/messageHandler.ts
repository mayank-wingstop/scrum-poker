import { WebSocket } from 'ws';
import { ClientMessage } from '@scrum-poker/shared';

// Stub — will be implemented in feature-specific coding stages
export function handleMessage(
  _ws: WebSocket,
  _participantId: string,
  _message: ClientMessage
): void {
  // TODO: implement per-feature
}
